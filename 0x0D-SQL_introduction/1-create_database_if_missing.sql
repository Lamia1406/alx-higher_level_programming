-- creates the database hbtn_0c_0 in your MySQL server
-- create a new database in MySQL, IF NOT EXISTS prevents an error from occurring if the database already exists.
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
